#include <stdio.h>
int main(){
    int R1[100], R2, R3;
    printf("저항을 입력해주세요: ");
    scanf("%s", &R2);
    
    if(R1 < 0){
        printf("잘못된 입력입니다.");
        return 0;
    }
    if else (R1 ){
        printf("잘못된 입력입니다.");
        return 0;
    }
    if else (R1 < ){
    }
    return 0;
}

